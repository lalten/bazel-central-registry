matrix:
  platform:
    - ubuntu2004_arm64
    - ubuntu2404
  bazel: [7.x, 8.x, latest]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@appimage_type2-runtime//:runtimes"
